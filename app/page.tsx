import Card from "@/components/Card/Card";

const Home: React.FC = () => {
  return (
    <main className="flex main-bg min-h-screen flex-col items-center justify-center">
      <Card />
    </main>
  );
};

export default Home;
